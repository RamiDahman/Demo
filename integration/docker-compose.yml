version: '3.1'

services:
  generator:    
    image: my-registry:55000/gen:ci-${GENERATOR_BUILD_NUMBER}
    ports:
      - "8081:80"
  integration: 
    image: my-registry:55000/gen:integration-${BUILD_NUMBER}
    build: .
    environment:
      - TEAMCITY_PROJECT_NAME: ${TEAMCITY_PROJECT_NAME}
  
      